/*
    test> const results = db.movies.find().toArray()

    test> console.table(results)
*/

// Note: This will not work in MongoDB shell.

// ALTERNATIVE:

/*
    - Go to MongoDB Compass
    - Go to Aggregations tab
    - Select Export
    - Select "CSV"
    - Click "Export"
*/